#!/bin/bash
echo "This repository contains multiple Terraform modules. Run each module separately, e.g.:"
echo "cd Q1_VPC_Networking && terraform init && terraform apply -var='name_prefix=Tushar_Kumar' -var='aws_region=ap-south-1'"
